"""Placeholder for a future Anthropic cloud backend.

Deprioritised for v1 — see ADR-004 in docs/decisions.md. The v1 LLM
target is a local runtime (Ollama). This file is intentionally kept
as a named stub so the import path exists for later cloud-fallback
work.
"""
